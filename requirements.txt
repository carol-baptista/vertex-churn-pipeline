# Secondary to pyproject.toml + uv.lock (the source of truth).
# Kept for users without uv: pip install -r requirements.txt
# Preferred: `uv sync`

# Core ML
pandas>=2.0,<3
scikit-learn>=1.3,<2
xgboost>=2.0,<3
joblib>=1.3,<2

# GCP
google-cloud-bigquery>=3.20,<4
google-cloud-bigquery-storage>=2.24,<3
google-cloud-storage>=2.14,<3
google-cloud-aiplatform>=1.50,<2
db-dtypes>=1.2,<2
pyarrow>=15.0

# EDA & viz
matplotlib>=3.8,<4
seaborn>=0.13,<1

# Local dev
python-dotenv>=1.0,<2
jupyter>=1.0,<2
ipykernel>=6.29,<7
